#include <bits/stdc++.h>
using namespace std;

#define nl '\n'
typedef long long int ll;
typedef  unsigned long long int llu;

int main ()
{
    ll n, k;
    cin >> n >> k;

    int a[n];

    for (ll i = 0; i < n; i++){
        cin >> a[i];
    }

    return 0;
}
